# Terraform state and local overrides — never commit these
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json
